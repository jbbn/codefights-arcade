/**
 * Retorna a soma de dois números
 * @param {number} a - Um número inteiro
 * @param {number} b - Um número inteiro
 * @return {number} - A soma dos números fornecidos
*/
const add = (a, b) =>  a + b

{
  // Forma alternativa utilizando ES5
  function add (a, b) {
    return a + b
  }
}
